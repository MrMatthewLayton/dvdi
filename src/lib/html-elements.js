import {h} from "./dvdi.js"

export const anchor = (props, ...childNodes) => h('a', props, ...childNodes);
export const abbreviation = (props, ...childNodes) => h('abbr', props, ...childNodes);
export const address = (props, ...childNodes) => h('address', props, ...childNodes);
export const area = (props, ...childNodes) => h('area', props, ...childNodes);
export const article = (props, ...childNodes) => h('article', props, ...childNodes);
export const aside = (props, ...childNodes) => h('aside', props, ...childNodes);
export const audio = (props, ...childNodes) => h('audio', props, ...childNodes);
export const base = (props, ...childNodes) => h('base', props, ...childNodes);
export const bidirectionalIsolate = (props, ...childNodes) => h('bdi', props, ...childNodes);
export const bidirectionalOverride = (props, ...childNodes) => h('bdo', props, ...childNodes);
export const blockquote = (props, ...childNodes) => h('blockquote', props, ...childNodes);
export const body = (props, ...childNodes) => h('body', props, ...childNodes);
export const breakLine = (props, ...childNodes) => h('br', props, ...childNodes);
export const button = (props, ...childNodes) => h('button', props, ...childNodes);
export const canvas = (props, ...childNodes) => h('canvas', props, ...childNodes);
export const caption = (props, ...childNodes) => h('caption', props, ...childNodes);
export const cite = (props, ...childNodes) => h('cite', props, ...childNodes);
export const code = (props, ...childNodes) => h('code', props, ...childNodes);
export const column = (props, ...childNodes) => h('col', props, ...childNodes);
export const columnGroup = (props, ...childNodes) => h('colgroup', props, ...childNodes);
export const data = (props, ...childNodes) => h('data', props, ...childNodes);
export const dataList = (props, ...childNodes) => h('datalist', props, ...childNodes);
export const definitionDescription = (props, ...childNodes) => h('dd', props, ...childNodes);
export const deletedText = (props, ...childNodes) => h('del', props, ...childNodes);
export const details = (props, ...childNodes) => h('details', props, ...childNodes);
export const definitionTerm = (props, ...childNodes) => h('dt', props, ...childNodes);
export const definition = (props, ...childNodes) => h('dfn', props, ...childNodes);
export const dialog = (props, ...childNodes) => h('dialog', props, ...childNodes);
export const div = (props, ...childNodes) => h('div', props, ...childNodes);
export const descriptionList = (props, ...childNodes) => h('dl', props, ...childNodes);
export const emphasis = (props, ...childNodes) => h('em', props, ...childNodes);
export const embed = (props, ...childNodes) => h('embed', props, ...childNodes);
export const fieldset = (props, ...childNodes) => h('fieldset', props, ...childNodes);
export const figureCaption = (props, ...childNodes) => h('figcaption', props, ...childNodes);
export const figure = (props, ...childNodes) => h('figure', props, ...childNodes);
export const footer = (props, ...childNodes) => h('footer', props, ...childNodes);
export const form = (props, ...childNodes) => h('form', props, ...childNodes);
export const header = (props, ...childNodes) => h('header', props, ...childNodes);
export const heading1 = (props, ...childNodes) => h('h1', props, ...childNodes);
export const heading2 = (props, ...childNodes) => h('h2', props, ...childNodes);
export const heading3 = (props, ...childNodes) => h('h3', props, ...childNodes);
export const heading4 = (props, ...childNodes) => h('h4', props, ...childNodes);
export const heading5 = (props, ...childNodes) => h('h5', props, ...childNodes);
export const heading6 = (props, ...childNodes) => h('h6', props, ...childNodes);
export const horizontalRule = (props, ...childNodes) => h('hr', props, ...childNodes);
export const html = (props, ...childNodes) => h('html', props, ...childNodes);
export const inlineFrame = (props, ...childNodes) => h('iframe', props, ...childNodes);
export const image = (props, ...childNodes) => h('img', props, ...childNodes);
export const input = (props, ...childNodes) => h('input', props, ...childNodes);
export const insertedText = (props, ...childNodes) => h('ins', props, ...childNodes);
export const keyboardInput = (props, ...childNodes) => h('kbd', props, ...childNodes);
export const label = (props, ...childNodes) => h('label', props, ...childNodes);
export const legend = (props, ...childNodes) => h('legend', props, ...childNodes);
export const listItem = (props, ...childNodes) => h('li', props, ...childNodes);
export const link = (props, ...childNodes) => h('link', props, ...childNodes);
export const main = (props, ...childNodes) => h('main', props, ...childNodes);
export const map = (props, ...childNodes) => h('map', props, ...childNodes);
export const markedText = (props, ...childNodes) => h('mark', props, ...childNodes);
export const meta = (props, ...childNodes) => h('meta', props, ...childNodes);
export const meter = (props, ...childNodes) => h('meter', props, ...childNodes);
export const navigation = (props, ...childNodes) => h('nav', props, ...childNodes);
export const noScript = (props, ...childNodes) => h('noscript', props, ...childNodes);
export const object = (props, ...childNodes) => h('object', props, ...childNodes);
export const orderedList = (props, ...childNodes) => h('ol', props, ...childNodes);
export const optionGroup = (props, ...childNodes) => h('optgroup', props, ...childNodes);
export const option = (props, ...childNodes) => h('option', props, ...childNodes);
export const output = (props, ...childNodes) => h('output', props, ...childNodes);
export const paragraph = (props, ...childNodes) => h('p', props, ...childNodes);
export const parameter = (props, ...childNodes) => h('param', props, ...childNodes);
export const picture = (props, ...childNodes) => h('picture', props, ...childNodes);
export const preformatted = (props, ...childNodes) => h('pre', props, ...childNodes);
export const progress = (props, ...childNodes) => h('progress', props, ...childNodes);
export const quotation = (props, ...childNodes) => h('q', props, ...childNodes);
export const rubyParenthesis = (props, ...childNodes) => h('rp', props, ...childNodes);
export const rubyText = (props, ...childNodes) => h('rt', props, ...childNodes);
export const ruby = (props, ...childNodes) => h('ruby', props, ...childNodes);
export const sampleOutput = (props, ...childNodes) => h('samp', props, ...childNodes);
export const script = (props, ...childNodes) => h('script', props, ...childNodes);
export const section = (props, ...childNodes) => h('section', props, ...childNodes);
export const select = (props, ...childNodes) => h('select', props, ...childNodes);
export const smallText = (props, ...childNodes) => h('small', props, ...childNodes);
export const source = (props, ...childNodes) => h('source', props, ...childNodes);
export const span = (props, ...childNodes) => h('span', props, ...childNodes);
export const strongText = (props, ...childNodes) => h('strong', props, ...childNodes);
export const style = (props, ...childNodes) => h('style', props, ...childNodes);
export const subscript = (props, ...childNodes) => h('sub', props, ...childNodes);
export const summary = (props, ...childNodes) => h('summary', props, ...childNodes);
export const superscript = (props, ...childNodes) => h('sup', props, ...childNodes);
export const table = (props, ...childNodes) => h('table', props, ...childNodes);
export const tableBody = (props, ...childNodes) => h('tbody', props, ...childNodes);
export const tableCell = (props, ...childNodes) => h('td', props, ...childNodes);
export const textArea = (props, ...childNodes) => h('textarea', props, ...childNodes);
export const tableFoot = (props, ...childNodes) => h('tfoot', props, ...childNodes);
export const tableHeaderCell = (props, ...childNodes) => h('th', props, ...childNodes);
export const tableHead = (props, ...childNodes) => h('thead', props, ...childNodes);
export const time = (props, ...childNodes) => h('time', props, ...childNodes);
export const title = (props, ...childNodes) => h('title', props, ...childNodes);
export const tableRow = (props, ...childNodes) => h('tr', props, ...childNodes);
export const track = (props, ...childNodes) => h('track', props, ...childNodes);
export const underline = (props, ...childNodes) => h('u', props, ...childNodes);
export const unorderedList = (props, ...childNodes) => h('ul', props, ...childNodes);
export const variable = (props, ...childNodes) => h('var', props, ...childNodes);
export const video = (props, ...childNodes) => h('video', props, ...childNodes);
export const wordBreakOpportunity = (props, ...childNodes) => h('wbr', props, ...childNodes);
